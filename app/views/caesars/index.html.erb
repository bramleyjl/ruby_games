<!DOCTYPE html>
<html>
<head>
  <title>Caesar Cipher</title>
</head>
<body>

<h1>Caesar Ciphers</h1>

<h3><%= link_to 'New Caesar Cipher', new_caesar_path %></h3>

<table>
  <thead>
    <tr>
      <td>Original Message</td>
      <td>Cipher Key</td>
      <td>Coded Message</td>
    </tr>
  </thead>

  <tbody>
    <% @caesars.each do |caesar| %>
      <tr>
        <td><%= caesar.message %></td>
        <td><%= caesar.key %></td>
        <td><%= caesar.code %></td>
        <td><%= link_to 'Show', caesar %></td>
        <td><%= link_to 'Edit', edit_caesar_path(caesar) %></td>
        <td><%= link_to 'Destroy', caesar, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

</body>
</html>